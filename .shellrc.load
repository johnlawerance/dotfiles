# include what you want

export PATH="$PATH:$HOME/bin:$HOME/.rvm/bin"

# add this to the .bashrc or .zshrc (done automatically by dfm):
# . $HOME/.shellrc.load

####
# put any bash/zsh customizations in here

[ -f /home/jclark/.travis/travis.sh ] && source /home/jclark/.travis/travis.sh
